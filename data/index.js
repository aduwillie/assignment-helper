const { assignmentTypes } = require("./assignment.types");
const { assignments } = require("./assignments");
const { data } = require("./test.data");

module.exports = { assignmentTypes, assignments, data };
